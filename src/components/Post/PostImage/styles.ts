import styled from 'styled-components';

export const PostImageContainer = styled.div`
  width: 100%;
  max-height: 170px;
  overflow: hidden;
  img {
    width: 100%;
    object-fit: contain;
  }
`;
